# action.yml
name: 'Alerts'
description: 'Application Insights Alerts'
inputs:
  app-id:  # id of input
    description: 'The Application ID'
    required: true
  api-key:
    description: 'The API key'
    required: true
  query:
    description: 'The Analytics query'
    required: true
  timespan:
    description: 'The timespan over which to query data'
    required: false
    default: PT1H
outputs:
  timestamp: # id of output
    description: 'Time'
  exceptionType: # id of output
    description: 'Type'
  method: # id of output
    description: 'Method'
  message: # id of output
    description: 'Message'
  assembly: # id of output
    description: 'Assembly'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.app-id }}
    - ${{ inputs.api-key }}
    - ${{ inputs.query }}
    - ${{ inputs.timespan }}